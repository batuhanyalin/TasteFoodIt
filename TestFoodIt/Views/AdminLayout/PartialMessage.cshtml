@model List<TestFoodIt.Entities.Contact>
@{ int count = 0;
    string countColor = ""; }
<li class="nav-item dropdown no-arrow mx-1">
    <a class="nav-link dropdown-toggle" href="#" id="alertsDropdown" role="button" data-toggle="dropdown"
       aria-haspopup="true" aria-expanded="false">
        @if (ViewBag.count == 0)
        {
            countColor = "badge badge-success badge-counter";
        }
        else
        {
            countColor = "badge badge-danger badge-counter";

        }
        <i class="fas fa-envelope fa-fw"></i>
        <span class="@countColor">@ViewBag.count</span>
    </a>
    <div class="dropdown-list dropdown-menu dropdown-menu-right shadow animated--grow-in"
         aria-labelledby="alertsDropdown">
        <h6 class="dropdown-header">
            Mesajlar
        </h6>
        @foreach (var item in Model)
        {
            count++;
            if (count <= 5)
            {
<a class="dropdown-item d-flex align-items-center" href="/Contact/ContactUpdate/@item.ContactId">
    <div class="mr-3">
        <div class="icon-circle bg-warning">
            <i class="far fa-comment-alt text-white"></i>
        </div>
    </div>
    <div>
        <div class="small text-gray-500">@item.SendDate</div>
        @if (@item.IsRead != true)
        {
<span class="font-weight-bold">@item.Message</span> }
else
{
<span class="font-weight-normal">@item.Message</span>}
    </div>
</a>}
else
{

}
}
        <a class="dropdown-item text-center small text-gray-500" href="/Contact/ContactList">Tüm Mesajları Göster</a>
    </div>
</li>


